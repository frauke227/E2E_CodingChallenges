# Testing Strings
# Create a function that takes a string as input (lower case) and returns the string with upper case letters, e.g. convert("small") returns: "SMALL".


def convert(word):
    return str(word).upper()


print(convert('Frauke'))
